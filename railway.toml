# Railway Configuration - Servizio Unificato (Backend + Frontend)
# Usa Dockerfile.unified che combina backend e frontend in un unico servizio

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.unified"
