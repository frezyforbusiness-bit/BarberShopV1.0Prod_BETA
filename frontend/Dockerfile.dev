# Frontend Dockerfile per Railway (modalità dev con Vite)
# Usa questo se preferisci Vite dev server invece di Nginx
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

RUN npm ci --legacy-peer-deps || npm install --legacy-peer-deps

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

